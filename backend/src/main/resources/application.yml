spring:
  application:
    name: qlc-stream-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/qlc_stream}
    username: ${DB_USER:qlc_stream}
    password: ${DB_PASSWORD:qlc_stream_local}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

qlc-stream:
  tmdb:
    base-url: https://api.themoviedb.org/3
    api-token: ${TMDB_API_TOKEN:}
  prowlarr:
    base-url: ${PROWLARR_BASE_URL:}
    api-key: ${PROWLARR_API_KEY:}
  qbittorrent:
    base-url: ${QBITTORRENT_BASE_URL:}
    username: ${QBITTORRENT_USERNAME:}
    password: ${QBITTORRENT_PASSWORD:}
  storage:
    media-root: ${MEDIA_ROOT:./data/media}
